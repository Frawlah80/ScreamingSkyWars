name: Extract and Move

on:
  push:
    branches:
      - main

jobs:
  extract_and_move:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Extract BedWars.zip
      run: |
        unzip BedWars.zip
